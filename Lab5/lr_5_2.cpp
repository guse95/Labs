#include <bits/stdc++.h>

class encoder {
public:

    explicit encoder(std::vector<std::byte> key) {
        mutator = key;
    }

    void encode(std::string in, std::string out, bool flag) {

    }

private:
    std::vector<std::byte> mutator;

};

int main() {

}